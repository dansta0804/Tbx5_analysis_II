## Tbx5 transcription factor analysis (continuation)

```{r Libraries}
# Loading used libraries:
library(pacman)
p_load(data.table, rtracklayer, ggplot2, ggthemes, reshape2, dplyr, Biostrings)
```

```{r Paths}
# Declaring paths:
INTERMEDIATE_FILES <- "/home/daniele/Desktop/Darbas/Tbx5_analysis_cont/Intermediate_data_files/"
INPUTS <- "/home/daniele/Desktop/Darbas/Tbx5_analysis_cont/Inputs/"
MOTIFS <- "/home/daniele/HOMER/bin/results/PEAKS054908_Tbx5_P70326_MACS2_12/knownResults/"
```

```{r Peaks_in_samples, fig.width = 4, fig.height = 6, fig.align = "center"}
# Creating Sample Key table:
samples <- read.csv(file = paste0(INTERMEDIATE_FILES, "sample_key.csv"))

# Importing BigBed format files:
FASTA <- list.files(path = paste0(INPUTS, "FASTA/"), "*fasta")
PWMs <- vector(mode = 'list', length = 25)

# Reading 25 Homer PWM matrix files from 'knownResults' directory:
for (pwm_matrix in seq(25)) {
    pwm <- read.table(paste0(MOTIFS, "known", pwm_matrix, ".motif"))
    PWMs[[pwm_matrix]] <- t(pwm) %>% `rownames<-`(c("A", "C", "G", "T"))
}

PWMs[[1]]
```